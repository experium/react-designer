{
  "private": true,
  "scripts": {
    "start": "yarn --cwd designer start & yarn --cwd example start",
    "build": "yarn --cwd designer build",
    "build:example": "yarn build & yarn --cwd example build"
  },
  "workspaces": [
    "designer",
    "example"
  ]
}
